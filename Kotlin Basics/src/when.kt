fun main(args : Array<String>)
{
    var x : Int =4 //define value
     when(x){   //apply when function
         0,1 -> print("x==0 or x==1")// apply case
         else ->print("otherwise")
     }
}